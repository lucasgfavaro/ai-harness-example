spring.application.name=home-agent-harness
server.port=${SERVER_PORT:8081}

# URL base del simulador de casa (sin trailing slash)
home.api.base-url=${HOME_API_BASE_URL:http://localhost:8080/api}

openai.model=${OPENAI_MODEL:gpt-4.1-mini}
agent.max-turns=8
